
struct User{
    id: u64,
    availables: Vec<bool>,
}

struct Spare{
    stamp: u64,
    day: u64,
}

// users: questionaire, spares
// return each user with the spares they can take with algorithm result
fn something(users: Vec<User>, spares: Vec<Spare>) -> Vec<User> {
    todo!()
}